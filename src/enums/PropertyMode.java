package enums;

/**
 * Represents the intended transaction mode of a property listing.
 */
public enum PropertyMode {
    FOR_SALE,       // Available for purchase
    FOR_RENT,       // Available for renting
    FOR_RENT_OUT    // Owner wants to rent it out through an agent
}
